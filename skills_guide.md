# الدليل الشامل والمفصل لكل مهارات الـ Agent الـ 40 (Complete Skills Breakdown)

هذا المستند يشرح **جميع المهارات الـ 40 بدون استثناء** بنفس مستوى التفصيل والعمق، موضحاً لكل مهارة:
- **ماذا تفعل المهارة بالتفصيل الدقيق؟**
- **متى تتفعل تلقائياً وتعمل؟ (Trigger Words)**
- **ما الذي ينتج عنها من كود ومجلدات؟ (Output)**

---

## 1. التأسيس والمعمارية (Scaffolding & Architecture)

### 1. `flutter-project-foundation`
- **الشرح التفصيلي**: مهارة التأسيس الأولى والأساسية. تُستخدم مرة واحدة عند بداية أي مشروع فلاتر جديد. تقوم بالتفاعل مع المستخدم لسؤاله عن تفضيلاته (شبكة، تخزين، لغة، ثيم، إدارة حالة)، تنظيف مشروع فلاتر الافتراضي (حذف العداد والـ `test/`)، ثم بناء الـ Core Skeleton كاملاً.
- **متى تُستدعى؟**: عندما تقول للـ Agent: *"أنشئ مشروع فلاتر جديد"* أو *"أسس لي هيكل المشروع"*.
- **الناتج عنها**:
  - مجلد `lib/core/` يحتوي على (`common/gap.dart`, `constants/app_constants.dart`, `di/app_initializer.dart`, `errors/`, `extensions/`, `theme/app_sizes.dart`, `utils/app_logger.dart`).
  - ملائمات `analysis_options.yaml` مخصصة بدون `very_good_analysis`.
  - ملف `README.md` باحترافية يحتوي على شارات الحماية (Badges) والعنوان المتغير.

### 2. `architecture-feature-first`
- **الشرح التفصيلي**: مهارة بناء وتصميم الميزات (Features). عندما تطلب إضافة ميزة جديدة للتطبيق، تمنع هذه المهارة الـ Agent من وضع الملفات بشكل عشوائي، وتجبره على وضع الكود داخل `lib/features/<feature_name>/` وتقسيمه إلى:
  - `data/`: تحتوي على `*_api_service.dart` و `*_repository.dart`.
  - `ui/`: تحتوي على `*_screen.dart` و `*_viewmodel.dart` (أو Cubit/Controller) ومجلد `widgets/`.
  - `domain/`: (اختياري) تحتوي على الـ `*_usecase.dart` للعمليات المعقدة فقط.
- **متى تُستدعى؟**: عندما تقول: *"أنشئ ميزة تسجيل الدخول Login"* أو *"أضف ميزة السلة Cart Feature"*.
- **الناتج عنها**: هيكل مجلدات مخصص للميزة وكود نظيف مفصول الطبقات.

### 3. `flutter-app-architecture`
- **الشرح التفصيلي**: مهارة المعمارية النظرية وقوانين تدفق البيانات (Unidirectional Data Flow). تفرض أن البيانات تتحرك في اتجاه واحد: الأحداث تطلع من الـ UI للـ ViewModel، والـ ViewModel يطلب من الـ Repository، والـ Repository هو المصدر الوحيد للحقيقة (SSOT). كما تمنع الـ UI من الاتصال المباشر بالـ Service.
- **متى تُستدعى؟**: عند تصميم المعمارية العامة للتطبيق، أو إعادة هيكلة كود قديم (Refactoring) وتطبيق نمط MVVM الرسمي من فلاتر.
- **الناتج عنها**: تطبيق صارم لـ Separation of Concerns ومنع التداخل بين الطبقات.

---

## 2. إدارة الحالة (State Management)

### 4. `bloc`
- **الشرح التفصيلي**: المهارة الخاصة بـ Flutter Bloc و Cubit. تضمن استخدام Sealed Classes للـ States، استخدام `BlocSelector` لمنع إعادات البناء غير الضرورية، وإضافة حماية `if (isClosed) return;` قبل أي `emit` بعد العمليات اللاتزامية (Async gap) لمنع الكراش عند إغلاق الشاشة.
- **متى تُستدعى؟**: عند طلب إنشاء Cubit أو Bloc جديد أو كتابة حالة باستخدام الـ Bloc.
- **الناتج عنها**: ملفات `*_cubit.dart` أو `*_bloc.dart` مع `*_state.dart` مكتوبة بأعلى معايير الأمان.

### 5. `riverpod`
- **الشرح التفصيلي**: المهارة الخاصة بمكتبة Riverpod (الإصدار 2.x). تفرض استخدام `Notifier` و `AsyncNotifier` مع Code Generation، والتعامل الصحيح مع `ref.watch` داخل الـ `build` و `ref.read` داخل الـ Callbacks، وتجنب الـ Memory Leaks بإدارة الـ AutoDispose.
- **متى تُستدعى؟**: عند بناء مشروع أو ميزة تعتمد على Riverpod.
- **الناتج عنها**: `StateNotifier` أو `NotifierProvider` مع كود نظيف ومحمي.

### 6. `provider`
- **الشرح التفصيلي**: المهارة الخاصة بمكتبة Provider التقليدية. تشرح كيفية تركيب الـ `MultiProvider` في أعلى الشجرة، الاستفادة من `Consumer` لإعادة بناء الجزء المحتاج فقط من الواجهة، واستخدام `ProxyProvider` عندما يعتمد Provider على آخر.
- **متى تُستدعى؟**: عند العمل على مشروع يستخدم حزمة `provider`.
- **الناتج عنها**: نموذج إدارة حالة بـ Provider محسن بدون إعادات بناء زائدة.

### 7. `flutter-change-notifier`
- **الشرح التفصيلي**: مهارة إدارة الحالة البسيطة والمحلية باستخدام كلاس `ChangeNotifier` المدمج داخل فلاتر بدون استخدام أي مكتبة خارجية. تضمن استخدام `ListenableBuilder` أو `AnimatedBuilder` للاستماع للتغييرات.
- **متى تُستدعى؟**: عند بناء مكونات معزولة أو تطبيقات بسيطة لا تتطلب مكتبات إدارة حالة معقدة.
- **الناتج عنها**: كلاس `ChangeNotifier` خفيف وربط سلس بالواجهة.

---

## 3. الواجهات والتصميم (UI & Figma)

### 8. `figma-to-flutter`
- **الشرح التفصيلي**: تحويل التصاميم من Figma (سواء صور أو كود Dev Mode) إلى كود فلاتر. المهارة تترجم Auto Layout إلى `Row` و `Column` و `Gap` وتمنع استخدام `Stack` و `Positioned` إلا في حالة وجود عناصر متداخلة بالفعل (مثل Avatar عليه اونلاين بادج). كما تبحث عن الـ Widgets الموجودة سلفاً وتستخدمها بدلاً من إعادة كتابتها.
- **متى تُستدعى؟**: عندما تزود الـ Agent بتصميم وتقول: *"حوّل هذا التصميم لكود فلاتر"*.
- **الناتج عنها**: كود واجهات متجاوب ومطابق للتصميم ومرتبط بنظام الـ Tokens (`AppSizes`, `AppColors`).

### 9. `flutter-use-column-row-first`
- **الشرح التفصيلي**: تمنع الـ Agent من إعطاء أبعاد ثابتة (Fixed Width/Height) للـ Containers والـ Cards وتجبره على البدء دائماً بـ `Row`, `Column`, `Expanded`, `Flexible`, و `Spacer` لضمان تجاوب الواجهة مع كل أحجام الشاشات المختلفة.
- **متى تُستدعى؟**: تفعل تلقائياً في كل مرة يتم فيها بناء أي واجهة مستخدم (UI Widget).
- **الناتج عنها**: واجهات متجاوبة تعمل على الهواتف الصغيرة والكبيرة بدون Overflow.

### 10. `flutter-app-icons-splash`
- **الشرح التفصيلي**: تشرح خطوة بخطوة كيفية إعداد واستخدام مكتبتي `flutter_launcher_icons` لتوليد أيقونات التطبيق لجميع المنصات، و `flutter_native_splash` لإعداد شاشات البداية الرسمية (Native Splash Screen) على Android و iOS.
- **متى تُستدعى؟**: عند طلب *"غير أيقونة التطبيق"* أو *"أضف Splash Screen"*.
- **الناتج عنها**: إعداد ملفات الـ YAML وتشغيل أوامر التوليد لتجهيز الأيقونات والـ Splash.

### 11. `flutter-pre-caching`
- **الشرح التفصيلي**: مهارة تسريع وتحسين أداء تشغيل التطبيق. تقدم حلولاً لعمل Pre-loading سلفاً للصور والخطوط وملفات أنيميشن Lottie/Rive أو البيانات الأساسية أثناء شاشة البداية، لمنع الشاشات البيضاء أو التلعثم عند فتح الشاشات لأول مرة.
- **متى تُستدعى؟**: عند تحسين أداء تشغيل التطبيق (Web or Mobile Startup Optimization).
- **الناتج عنها**: كود Pre-caching يوضع بداخل `main.dart` أو شاشة البداية.

---

## 4. خدمات Firebase الشاملة (Firebase Stack)

### 12. `flutterfire-configure`
- **الشرح التفصيلي**: خطوة التأسيس الأولى لـ Firebase. تشرح كيفية تشغيل CLI `flutterfire configure` وتوليد `firebase_options.dart` وربط التطبيق على Android و iOS و Web، وإضافة `Firebase.initializeApp()`.
- **متى تُستدعى؟**: عند إضافة Firebase للمشروع لأول مرة.

### 13. `firebase-auth`
- **الشرح التفصيلي**: إدارة هويات المستخدمين. تتضمن طرق التسجيل بالإيميل وكلمة السر، التسجيل بحسابات التواصل (Google, Apple)، معالجة حالات الاستثناءات (FirebaseAuthException)، وإدارة جلسة المستخدم (Auth State Changes).
- **متى تُستدعى؟**: عند بناء ميزات التسجيل، تسجيل الدخول، وإعادة تعيين كلمة السر.

### 14. `firebase-cloud-firestore`
- **الشرح التفصيلي**: التعامل مع قاعدة بيانات Firestore. تشرح قواعد الـ CRUD، تحويل البيانات لـ Typed Models، استخدام `withConverter`، تفعيل الـ Offline Persistence، وإنشاء الاستعلامات والـ Pagination.
- **متى تُستدعى؟**: عند قراءة أو كتابة بيانات من وإلى Firestore.

### 15. `firebase-database`
- **الشرح التفصيلي**: التعامل مع Realtime Database. تُركز على مزامنة البيانات اللحظية فوراً (Live Sync)، التفاعل مع القوائم المباشرة، وحماية البيانات أثناء انقطاع الاتصال.
- **متى تُستدعى؟**: عند بناء ميزات مثل الشات اللحظي أو التتبع المباشر (Live Tracking).

### 16. `firebase-storage`
- **الشرح التفصيلي**: رفع وتنزيل الملفات (صور، فيديو، مستندات). تتضمن حساب نسبة التقدم في الرفع (Upload Progress Stream)، إضافة البيانات الوصفية (Metadata)، واستخراج روابط التحميل (Download URLs).
- **متى تُستدعى؟**: عند رفع صورة الملف الشخصي أو رفع مستندات.

### 17. `firebase-messaging`
- **الشرح التفصيلي**: إعداد وتفعيل إشعارات FCM. تتضمن طلب الصلاحيات من المستخدم، جلب الـ FCM Token، الاستماع للإشعارات في الأمامية (Foreground)، ومعالجة الضغط على الإشعارات في الخلفية (Background/Terminated).
- **متى تُستدعى؟**: عند تفعيل وتناول الإشعارات (Push Notifications).

### 18. `firebase-analytics`
- **الشرح التفصيلي**: تتبع سلوك المستخدمين داخل التطبيق. تتضمن تسجيل الأحداث المخصصة (Custom Events)، تحديد خصائص المستخدم (User Properties)، وتتبع فتح الشاشات تلقائياً (Screen Views).
- **متى تُستدعى؟**: عند طلب إحصائيات وتحليلات سلوك المستخدمين.

### 19. `firebase-crashlytics`
- **الشرح التفصيلي**: تتبع وتسجيل الأخطاء والكراشات. تقوم بربط `FlutterError.onError` و `PlatformDispatcher.instance.onError` لضمان إرسال أي خطأ غير متوقع مباشرة لـ Crashlytics.
- **متى تُستدعى؟**: عند تفعيل تقارير الأخطاء والاستثناءات في الإنتاج.

### 20. `firebase-remote-config`
- **الشرح التفصيلي**: التغيير الديناميكي في إعدادات التطبيق بدون تحديث المتجر. تشمل تحديد القيم الافتراضية المحلية (Defaults)، جلب وتفعيل الإعدادات (Fetch & Activate)، وتفعيل الـ Feature Flags.
- **متى تُستدعى؟**: عند عمل A/B Testing أو التحكم بفتح/إغلاق ميزات من الباك إند.

### 21. `firebase-app-check`
- **الشرح التفصيلي**: حماية الباك إند وخدمات Firebase من الهجمات والتطبيقات المزيفة. تضبط موفري الحماية (Play Integrity لـ Android، و App Attest/DeviceCheck لـ iOS، و Debug Tokens أثناء التطوير).
- **متى تُستدعى؟**: عند تأمين التطبيق وقواعد البيانات من الاستخدام غير المصرح به.

### 22. `firebase-cloud-functions`
- **الشرح التفصيلي**: استدعاء دوال السيرفر المكتوبة على Firebase Cloud Functions باستعمال `httpsCallable` وتناول الأخطاء والـ Timeouts بشكل آمن.
- **متى تُستدعى؟**: عند تشغيل عمليات سيرفر معقدة مثل الدفع أو إرسال إيميلات.

### 23. `firebase-in-app-messaging`
- **الشرح التفصيلي**: عرض الرسائل والعروض داخل التطبيق للمستخدمين النشطين (In-App Messages) والتحكم في ظهورها أو إخفائها بناءً على أحداث معينة.
- **متى تُستدعى؟**: عند عمل حملات ترويجية ورسائل تفاعلية داخل التطبيق.

### 24. `firebase-ai`
- **الشرح التفصيلي**: استخدام نماذج الذكاء الاصطناعي Gemini مباشرة داخل التطبيق عن طريق حزمة Firebase AI الرسمية وتوليد النصوص والدردشة.
- **متى تُستدعى؟**: عند دمج الذكاء الاصطناعي و Gemini في تطبيقات فلاتر.

### 25. `firebase-data-connect`
- **الشرح التفصيلي**: استخدام خدمة Firebase Data Connect الجديدة للربط مع قواعد بيانات PostgreSQL وكتابة استعلامات GraphQL واستخدام الـ SDK المولّد.
- **متى تُستدعى؟**: عند بناء تطبيقات تعتمد على Data Connect و GraphQL.

---

## 5. الذكاء الاصطناعي المتقدم (AI Agents)

### 26. `developing-genkit-dart`
- **الشرح التفصيلي**: مهارة متخصصة في بناء AI Agents كاملة ومتقدمة بلغة Dart باستخدام إطار عمل Genkit. تشرح كيفية تعريف الـ Flows، الأدوات (Tools)، والتعامل مع النماذج الذكية.
- **متى تُستدعى؟**: عند بناء نظام مساعد ذكي أو Agent متكامل داخل تطبيق فلاتر.
- **الناتج عنها**: كود Genkit Flows و Tools منظم ومستقر بلغة Dart.

---

## 6. الاختبار والجودة (Testing & QA)

### 27. `testing`
- **الشرح التفصيلي**: الدليل الأساسي لكتابة ومراجعة جميع أنواع الاختبارات في فلاتر (Unit Tests, Widget Tests, Golden Tests). تضمن التغطية الصحيحة ومعالجة الاختبارات المتذبذبة (Flaky Tests).
- **متى تُستدعى؟**: عند طلب كتابة اختبارات أو مراجعتها وتصحيح الفحوصات الفاشلة.
- **الناتج عنها**: ملفات اختبارات نظيفة ومستقرة في مجلد `test/`.

### 28. `mocktail`
- **الشرح التفصيلي**: استخدام مكتبة Mocktail لإنشاء كائنات وهمية (Mocks) في الاختبارات بدون الحاجة لتوليد كود (No Code Gen)، وتسجيل الـ Fallback Values والاستدعاءات.
- **متى تُستدعى؟**: عند كتابة Unit Tests وتزويدها بـ Mocks سريعة.

### 29. `mockito`
- **الشرح التفصيلي**: استخدام مكتبة Mockito التقليدية التي تعتمد على توليد الكود عبر `@GenerateMocks` وتحديد السلوك بـ `when().thenAnswer()`.
- **متى تُستدعى؟**: في المشاريع التي تستخدم Mockito مع `build_runner`.

### 30. `patrol-e2e-testing`
- **الشرح التفصيلي**: كتابة اختبارات شاملة من البداية للنهاية (E2E Integration Tests) باستخدام إطار عمل Patrol 4.x التفاعلي مع عناصر النظام الحقيقية (صلاحيات، كاميرا، حوارات النظام).
- **متى تُستدعى؟**: عند كتابة اختبارات تكاملية على مستوى التطبيق بالكامل.

### 31. `code-review`
- **الشرح التفصيلي**: مهارة تدقيق وتفتيش الكود (Code Review & Audit). عند استدعائها، تقوم بمسح الكود المكتوب وفحص الأخطاء، المشاكل في المعمارية، عدم التنسيق، وتزويدك بتقرير شامل للتعديلات.
- **متى تُستدعى؟**: عندما تقول: *"راجع لي الكود"* أو *"افحص الجودة في هذا التغيير"*.

### 32. `revenuecat-testing`
- **الشرح التفصيلي**: إعداد واختبار عمليات الشراء والاشتراكات داخل التطبيق (In-App Purchases) عبر مكتبة RevenueCat، واختبار البيئة التجريبية (Sandbox Testing) والتحقق من الصلاحيات (Entitlements).
- **متى تُستدعى؟**: عند اختبار الاشتراكات والدفع داخل التطبيق.

---

## 7. لغة Dart والأداء (Dart & Best Practices)

### 33. `dart-3-updates`
- **الشرح التفصيلي**: تحديث وعصرنة كود Dart القديم لاستخدام مميزات Dart 3 الحديثة مثل: Pattern Matching, Switch Expressions, Records, Destructuring, و Class Modifiers (`sealed`, `final`, `base`).
- **متى تُستدعى؟**: عند تحسين وإعادة كتابة الشروط المعقدة و Dart Code Modernization.

### 34. `effective-dart`
- **الشرح التفصيلي**: تطبيق القواعد الرسمية والتنسيقية للغة Dart (التسميات CamelCase و snake_case، التوثيق بالـ Doc Comments، ترتيب الـ Imports، ومنع المتغيرات التي بلا أنواع صريحة).
- **متى تُستدعى؟**: تفعل تلقائياً أثناء كتابة ومراجعة أي كود Dart.

### 35. `flutter-best-practices`
- **الشرح التفصيلي**: أفضل الممارسات العامة لبناء تطبيقات فلاتر مستقرة، وتشمل تنظيم المجلدات، كفاءة الـ Widgets، إدارة الـ Build Context الآمنة عبر الـ Async Gaps، والأداء.
- **متى تُستدعى؟**: تفعل كمرجع عام بكثرة أثناء كتابة وتطوير التطبيق.

### 36. `flutter-errors`
- **الشرح التفصيلي**: مهارة متخصصة في تشخيص وحل أخطاء الواجهات السريعة مثل: `RenderFlex overflowed by X pixels` أو `Vertical viewport was given unbounded height` أو أخطاء الاستدعاء أثناء الـ Build.
- **متى تُستدعى؟**: عند حدوث مشكلة أو خطأ في الواجهة (Layout/Scroll Error).

---

## 8. التصميم الشامل والوصول (Accessibility & Inclusion)

### 37. `accessibility`
- **الشرح التفصيلي**: دعم إتاحة التطبيق لذوي الهمم والاحتياجات الخاصة وفق معايير WCAG. تشمل إضافات `Semantics` على العناصر، التأكد من أن أزرار اللمس لا تقل عن 48x48 dp، دعم قارئ الشاشة، والتحكم بالتباين والخطوط.
- **متى تُستدعى؟**: عند تحسين الوصول والملاءمة لذوي الاحتياجات الخاصة (A11y).

### 38. `inclusive-design`
- **الشرح التفصيلي**: التصميم الشامل لدعم جميع أنواع المستخدمين والثقافات والأجهزة. تشمل دعم الاتجاهات (LTR/RTL)، دعم الأجهزة الضعيفة ذات الذاكرة المحدودة، والعمل على الشبكات البطيئة (Slow/Metered networks).
- **متى تُستدعى؟**: عند التخطيط لدعم تطبيقات عابرة للثقافات أو أجهزة منخفضة الأداء.

---

## 9. التطوير والرفع (Deployment & Skill Creation)

### 39. `store-listing-assets`
- **الشرح التفصيلي**: تجهيز أصول ومستلزمات الرفع للمتاجر (Google Play Console & App Store Connect). تشمل كتابة الوصف القصير والطويل والكلمات المفتاحية مع الالتزام التام بعدد الحروف المسموح به، وتجهيز لقطات الشاشة.
- **متى تُستدعى؟**: عند تجهيز التطبيق للنشر على المتاجر.

### 40. `create-skill`
- **الشرح التفصيلي**: المهارة الأم المسؤولة عن توليد وإنشاء مهارات جديدة لـ Agent الذكاء الاصطناعي. تقوم بإنشاء مجلد المهارة وقالب `SKILL.md` والـ References واحتساب الـ Hash وتحديث ملف `skills-lock.json` تلقائياً.
- **متى تُستدعى؟**: عندما تقول: *"اصنع لي مهارة جديدة عن X"*.
- **الناتج عنها**: مهارة جديدة متكاملة ومسجلة في المساعد الذكي جاهزة للاستخدام فوراً.

---
